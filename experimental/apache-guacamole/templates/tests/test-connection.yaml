apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "apache-guacamole.fullname" . }}-test-connection"
  labels:
    {{- include "apache-guacamole.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "apache-guacamole.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
